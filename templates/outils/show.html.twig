{% extends 'base.html.twig' %}

{% block title %}Outils{% endblock %}

{% block body %}
    <h1>Outils</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ outil.id }}</td>
            </tr>
            <tr>
                <th>Nom_outil</th>
                <td>{{ outil.nomOutil }}</td>
            </tr>
            <tr>
                <th>Descriptifs</th>
                <td>{{ outil.descriptifs }}</td>
            </tr>
            <tr>
                <th>Date_enregistrement</th>
                <td>{{ outil.dateEnregistrement ? outil.dateEnregistrement|date('d-m-y H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Duree_emprunt</th>
                <td>{{ outil.dureeEmprunt }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('outils_index') }}">back to list</a>

    <a href="{{ path('outils_edit', {'id': outil.id}) }}">edit</a>

    {{ include('outils/_delete_form.html.twig') }}
{% endblock %}
