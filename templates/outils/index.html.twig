{% extends 'base.html.twig' %}

{% block title %}Outils index{% endblock %}

{% block body %}
    <h1>Outils index</h1>
    <table class="table">
        <tbody>
        {% for outil in outils %}
           <div class="row">
            <div class="col-sm-4">
              <div class="card-deck">
                <div class="card">
                  <img src="{{ asset('build/images/' media.url_media ) }} " class="card-img-top" alt="...">
                  <div class="card-body">
                    <h5 class="card-title">{{ outil.nomOutil }}</h5>
                    <p class="card-text">T{{ outil.descriptifs }}</p>
                    <p class="card-text"><small class="text-muted">Disponible pendant {{ outil.dureeEmprunt }} jours</small></p>
                    <a href="{{ path('outils_show', {'id': outil.id}) }}">Détail</a>
                  </div>
                </div>
              </div>
            </div>
          </div>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>


{% endblock %}
